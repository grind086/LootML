export enum TOKEN_TYPE {
    ERROR,

    // Syntax elements
    COMMA,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    LEFT_BRACE,
    RIGHT_BRACE,
    HYPHEN,
    ALIAS,
    IDENTIFIER,
    COMMENT,

    // Basic types
    NUMBER,
    STRING
}
